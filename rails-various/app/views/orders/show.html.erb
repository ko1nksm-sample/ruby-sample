<h1>注文詳細</h1>

<dl>
  <dt>名前</dt>
  <dd><%= @order.name %></dd>
  <dt>住所</dt>
  <dd><%= @order.address %></dd>
</dl>

<table>
  <tr><td>商品<td>個数</tr>
  <% @order.details.each do |detail| %>
  <tr>
    <td><%= detail.product %></td>
    <td><%= detail.quantity %></td>
  </tr>
  <% end %>
</table>


<%= link_to '注文一覧', orders_path %>
